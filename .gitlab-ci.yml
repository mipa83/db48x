stages:
  - build
  - package

variables:
  GIT_SUBMODULE_STRATEGY: recursive

# macOS Simulator Builds
build-simulator-macos:
  stage: build
  tags:
    - macos
  before_script:
    - brew install qt@6
    - export PATH="/usr/local/opt/qt@6/bin:$PATH"
  script:
    - echo "$CI_PIPELINE_IID" > .build_id
    - make sim
    - macdeployqt sim/db48x.app
    - |
      if [ -n "$APPLE_CERT_DATA" ]; then
        echo "Using proper Apple Developer code signing"
        # Note: lando/code-sign-action is GitHub Actions specific
        # For GitLab, you'd need to implement similar signing logic or use a script
        # For now, fall back to ad-hoc signing
        codesign --force --deep --sign - sim/db48x.app
      else
        echo "Using ad-hoc code signing (no Apple Developer certificate)"
        codesign --force --deep --sign - sim/db48x.app
      fi
    - cp allow-db48x-macos.sh sim/
    - chmod +x sim/allow-db48x-macos.sh
    - cd sim && zip -r db48x-macos.zip db48x.app allow-db48x-macos.sh && cd ..
  artifacts:
    name: "db48x-simulator-macos-$CI_COMMIT_SHORT_SHA"
    paths:
      - sim/db48x-macos.zip
    expire_in: 30 days

build-color-simulator-macos:
  stage: build
  tags:
    - macos
  before_script:
    - brew install qt@6
    - export PATH="/usr/local/opt/qt@6/bin:$PATH"
  script:
    - echo "$CI_PIPELINE_IID" > .build_id
    - make color-dm32-sim
    - macdeployqt sim/db50x.app
    - |
      if [ -n "$APPLE_CERT_DATA" ]; then
        echo "Using proper Apple Developer code signing"
        # Note: lando/code-sign-action is GitHub Actions specific
        # For GitLab, you'd need to implement similar signing logic or use a script
        # For now, fall back to ad-hoc signing
        codesign --force --deep --sign - sim/db50x.app
      else
        echo "Using ad-hoc code signing (no Apple Developer certificate)"
        codesign --force --deep --sign - sim/db50x.app
      fi
    - cp allow-db50x-macos.sh sim/
    - chmod +x sim/allow-db50x-macos.sh
    - cd sim && zip -r db50x-macos.zip db50x.app allow-db50x-macos.sh && cd ..
  artifacts:
    name: "db50x-color-simulator-macos-$CI_COMMIT_SHORT_SHA"
    paths:
      - sim/db50x-macos.zip
    expire_in: 30 days

# Linux Simulator Builds
build-simulator-linux:
  stage: build
  image: fedora:latest
  tags:
    - docker
  before_script:
    - dnf install -y @development-tools git python3 python3-pip libxcb-devel mesa-libGL-devel libxkbcommon-x11-devel freetype-devel pkgconfig xcb-util-wm-devel xcb-util-image-devel xcb-util-keysyms-devel xcb-util-renderutil-devel
    - python3 -m pip install aqtinstall
    - python3 -m aqt install-qt linux desktop 6.5.3 --outputdir /opt/Qt --modules qtmultimedia
    - export PATH="/opt/Qt/6.5.3/gcc_64/bin:$PATH"
    - export QT_QPA_PLATFORM=offscreen
  script:
    - echo "$CI_PIPELINE_IID" > .build_id
    - make sim
  artifacts:
    name: "db48x-simulator-linux-$CI_COMMIT_SHORT_SHA"
    paths:
      - sim/db48x
      - help/
    expire_in: 30 days

build-color-simulator-linux:
  stage: build
  image: fedora:latest
  tags:
    - docker
  before_script:
    - dnf install -y @development-tools git python3 python3-pip libxcb-devel mesa-libGL-devel libxkbcommon-x11-devel freetype-devel pkgconfig xcb-util-wm-devel xcb-util-image-devel xcb-util-keysyms-devel xcb-util-renderutil-devel
    - python3 -m pip install aqtinstall
    - python3 -m aqt install-qt linux desktop 6.5.3 --outputdir /opt/Qt --modules qtmultimedia
    - export PATH="/opt/Qt/6.5.3/gcc_64/bin:$PATH"
    - export QT_QPA_PLATFORM=offscreen
  script:
    - echo "$CI_PIPELINE_IID" > .build_id
    - make color-dm32-sim
  artifacts:
    name: "db50x-color-simulator-linux-$CI_COMMIT_SHORT_SHA"
    paths:
      - sim/db50x
      - help/
    expire_in: 30 days

# Windows Simulator Builds
build-simulator-windows:
  stage: build
  tags:
    - windows
  cache:
    key: msys2-packages-v1
    paths:
      - C:\tools\msys64\var\cache\pacman\pkg\
  before_script:
    - choco install -y msys2
    - C:\tools\msys64\usr\bin\bash.exe -lc "pacman -S --noconfirm git mingw-w64-x86_64-gcc mingw-w64-x86_64-make mingw-w64-x86_64-freetype mingw-w64-x86_64-pkg-config mingw-w64-x86_64-libgnurx mingw-w64-x86_64-qt6-base mingw-w64-x86_64-qt6-declarative mingw-w64-x86_64-qt6-multimedia"
    - $env:PATH = "C:\tools\msys64\mingw64\bin;$env:PATH"
  script:
    - echo $env:CI_PIPELINE_IID > .build_id
    - mingw32-make sim
    - cd sim/debug
    - windeployqt6 db48x.exe
    - |
      # Copy MinGW runtime and MSYS2 dependencies
      Copy-Item C:\tools\msys64\mingw64\bin\libgcc_s_seh-1.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libstdc++-6.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libwinpthread-1.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libsystre-0.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libtre-5.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libintl-8.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libiconv-2.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libb2-1.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libdouble-conversion.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libicuin*.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libicuuc*.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libicudt*.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libpcre2-16-0.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\zlib1.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libzstd.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libfreetype-6.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libharfbuzz-0.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libmd4c.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libpng16-16.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libbrotlidec.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libbrotlicommon.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libbz2-1.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libglib-2.0-0.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libgraphite2.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libpcre2-8-0.dll .
    - cd ../..
  artifacts:
    name: "db48x-simulator-windows-$CI_COMMIT_SHORT_SHA"
    paths:
      - sim/debug/db48x.exe
      - sim/debug/*.dll
      - sim/debug/platforms/
      - sim/debug/imageformats/
      - sim/debug/tls/
      - help/
    expire_in: 30 days

build-color-simulator-windows:
  stage: build
  tags:
    - windows
  cache:
    key: msys2-packages-v1
    paths:
      - C:\tools\msys64\var\cache\pacman\pkg\
  before_script:
    - choco install -y msys2
    - C:\tools\msys64\usr\bin\bash.exe -lc "pacman -S --noconfirm git mingw-w64-x86_64-gcc mingw-w64-x86_64-make mingw-w64-x86_64-freetype mingw-w64-x86_64-pkg-config mingw-w64-x86_64-libgnurx mingw-w64-x86_64-qt6-base mingw-w64-x86_64-qt6-declarative mingw-w64-x86_64-qt6-multimedia"
    - $env:PATH = "C:\tools\msys64\mingw64\bin;$env:PATH"
  script:
    - echo $env:CI_PIPELINE_IID > .build_id
    - mingw32-make color-dm32-sim
    - cd sim/debug
    - windeployqt6 db50x.exe
    - |
      # Copy MinGW runtime and MSYS2 dependencies
      Copy-Item C:\tools\msys64\mingw64\bin\libgcc_s_seh-1.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libstdc++-6.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libwinpthread-1.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libsystre-0.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libtre-5.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libintl-8.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libiconv-2.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libb2-1.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libdouble-conversion.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libicuin*.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libicuuc*.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libicudt*.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libpcre2-16-0.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\zlib1.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libzstd.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libfreetype-6.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libharfbuzz-0.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libmd4c.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libpng16-16.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libbrotlidec.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libbrotlicommon.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libbz2-1.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libglib-2.0-0.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libgraphite2.dll .
      Copy-Item C:\tools\msys64\mingw64\bin\libpcre2-8-0.dll .
    - cd ../..
  artifacts:
    name: "db50x-color-simulator-windows-$CI_COMMIT_SHORT_SHA"
    paths:
      - sim/debug/db50x.exe
      - sim/debug/*.dll
      - sim/debug/platforms/
      - sim/debug/imageformats/
      - sim/debug/tls/
      - help/
    expire_in: 30 days

# WebAssembly Build
build-wasm:
  stage: build
  image: fedora:latest
  tags:
    - docker
  before_script:
    - dnf install -y @development-tools git python3 curl freetype-devel pkgconfig
    - git clone https://github.com/emscripten-core/emsdk.git
    - cd emsdk
    - ./emsdk install latest
    - ./emsdk activate latest
    - cd ..
  script:
    - source emsdk/emsdk_env.sh
    - echo "$CI_PIPELINE_IID" > .build_id
    - make SHELL=/bin/bash wasm
  artifacts:
    name: "db48x-wasm-$CI_COMMIT_SHORT_SHA"
    paths:
      - wasm/db48x.js
      - wasm/db48x.wasm
      - wasm/*.html
      - help/
    expire_in: 30 days

# Android Builds
build-android:
  stage: build
  image: fedora:latest
  tags:
    - docker
  before_script:
    - dnf install -y @development-tools git java-17-openjdk-devel wget unzip freetype-devel pkgconfig
    - export JAVA_HOME=/usr/lib/jvm/java-17-openjdk
    - export PATH=$JAVA_HOME/bin:$PATH
    # Install Android SDK
    - wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
    - mkdir -p $HOME/android-sdk/cmdline-tools
    - unzip -q commandlinetools-linux-11076708_latest.zip -d $HOME/android-sdk/cmdline-tools
    - mv $HOME/android-sdk/cmdline-tools/cmdline-tools $HOME/android-sdk/cmdline-tools/latest
    - export ANDROID_SDK_ROOT=$HOME/android-sdk
    - export PATH=$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$PATH
    - yes | sdkmanager --licenses || true
    - sdkmanager --install "ndk;26.1.10909125" "platforms;android-35" "build-tools;35.0.0"
    - export ANDROID_NDK_ROOT=$ANDROID_SDK_ROOT/ndk/26.1.10909125
    # Install Qt for Android
    - python3 -m pip install aqtinstall
    - python3 -m aqt install-qt linux android 6.8.1 android_arm64_v8a --outputdir $HOME/Qt --modules qtmultimedia
    - python3 -m aqt install-qt linux desktop 6.8.1 gcc_64 --outputdir $HOME/Qt
    - export Qt6_DIR=$HOME/Qt/6.8.1/android_arm64_v8a
    - export QT_HOST_PATH=$HOME/Qt/6.8.1/gcc_64
  script:
    - echo "$CI_PIPELINE_IID" > .build_id
    - make sim/config.qrc sim/state.qrc sim/library.qrc sim/help.qrc sim/help/img.qrc
    - cd recorder && make TARGET=opt && cd ..
    - |
      if [ -n "$ANDROID_KEYSTORE_DATA" ]; then
        mkdir -p $HOME/.local
        echo "$ANDROID_KEYSTORE_DATA" | base64 -d > $HOME/.local/android_release.keystore
        make android
      else
        cd sim && \
          $Qt6_DIR/bin/qmake -spec android-clang db48x.pro && \
          make VARIANT=android && \
          mkdir -p ../android/libs/arm64-v8a && \
          cp libdb48x_arm64-v8a.so ../android/libs/arm64-v8a/ && \
          $QT_HOST_PATH/bin/androiddeployqt \
            --input android-db48x-deployment-settings.json \
            --output ../android --gradle --aab
      fi
  artifacts:
    name: "db48x-android-$CI_COMMIT_SHORT_SHA"
    paths:
      - android/*.aab
      - help/
    expire_in: 30 days

build-color-android:
  stage: build
  image: fedora:latest
  tags:
    - docker
  before_script:
    - dnf install -y @development-tools git java-17-openjdk-devel wget unzip freetype-devel pkgconfig
    - export JAVA_HOME=/usr/lib/jvm/java-17-openjdk
    - export PATH=$JAVA_HOME/bin:$PATH
    # Install Android SDK
    - wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
    - mkdir -p $HOME/android-sdk/cmdline-tools
    - unzip -q commandlinetools-linux-11076708_latest.zip -d $HOME/android-sdk/cmdline-tools
    - mv $HOME/android-sdk/cmdline-tools/cmdline-tools $HOME/android-sdk/cmdline-tools/latest
    - export ANDROID_SDK_ROOT=$HOME/android-sdk
    - export PATH=$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$PATH
    - yes | sdkmanager --licenses || true
    - sdkmanager --install "ndk;26.1.10909125" "platforms;android-35" "build-tools;35.0.0"
    - export ANDROID_NDK_ROOT=$ANDROID_SDK_ROOT/ndk/26.1.10909125
    # Install Qt for Android
    - python3 -m pip install aqtinstall
    - python3 -m aqt install-qt linux android 6.8.1 android_arm64_v8a --outputdir $HOME/Qt --modules qtmultimedia
    - python3 -m aqt install-qt linux desktop 6.8.1 gcc_64 --outputdir $HOME/Qt
    - export Qt6_DIR=$HOME/Qt/6.8.1/android_arm64_v8a
    - export QT_HOST_PATH=$HOME/Qt/6.8.1/gcc_64
  script:
    - echo "$CI_PIPELINE_IID" > .build_id
    - make COLOR=color sim/config.qrc sim/state.qrc sim/library.qrc sim/help.qrc sim/help/img.qrc
    - cd recorder && make TARGET=opt && cd ..
    - |
      if [ -n "$ANDROID_KEYSTORE_DATA" ]; then
        mkdir -p $HOME/.local
        echo "$ANDROID_KEYSTORE_DATA" | base64 -d > $HOME/.local/android_release.keystore
        make color-android
      else
        cd sim && \
          $Qt6_DIR/bin/qmake -spec android-clang db50x.pro && \
          make VARIANT=android && \
          mkdir -p ../android/libs/arm64-v8a && \
          cp libdb50x_arm64-v8a.so ../android/libs/arm64-v8a/ && \
          $QT_HOST_PATH/bin/androiddeployqt \
            --input android-db50x-deployment-settings.json \
            --output ../android --gradle --aab
      fi
  artifacts:
    name: "db50x-android-$CI_COMMIT_SHORT_SHA"
    paths:
      - android/*.aab
      - help/
    expire_in: 30 days

# DM42 Firmware Build
build-dm42-firmware:
  stage: build
  image: fedora:latest
  tags:
    - docker
  before_script:
    - dnf install -y @development-tools git arm-none-eabi-gcc-cs arm-none-eabi-binutils-cs arm-none-eabi-newlib freetype-devel pkgconfig
  script:
    - echo "$CI_PIPELINE_IID" > .build_id
    - make all || make all
    - make dist || make dist
  artifacts:
    name: "db48x-dm42-firmware-$CI_COMMIT_SHORT_SHA"
    paths:
      - db48x-v*.tgz
    expire_in: 30 days

# DM32 Firmware Build
build-dm32-firmware:
  stage: build
  image: fedora:latest
  tags:
    - docker
  before_script:
    - dnf install -y @development-tools git arm-none-eabi-gcc-cs arm-none-eabi-binutils-cs arm-none-eabi-newlib freetype-devel pkgconfig
  script:
    - echo "$CI_PIPELINE_IID" > .build_id
    - make dm32-all || make dm32-all
    - make dm32-dist || make dm32-dist
    - mv db50x-v*.tgz db50x-dm32-firmware.tgz
  artifacts:
    name: "db50x-dm32-firmware-$CI_COMMIT_SHORT_SHA"
    paths:
      - db50x-dm32-firmware.tgz
    expire_in: 30 days

build-dm42n-firmware:
  stage: build
  image: fedora:latest
  tags:
    - docker
  before_script:
    - dnf install -y @development-tools git arm-none-eabi-gcc-cs arm-none-eabi-binutils-cs arm-none-eabi-newlib freetype-devel pkgconfig
  script:
    - echo "$CI_PIPELINE_IID" > .build_id
    - make dm42n-all || make dm42n-all
    - make dm42n-dist || make dm42n-dist
    - mv db50x-v*.tgz db50x-dm42n-firmware.tgz
  artifacts:
    name: "db50x-dm42n-firmware-$CI_COMMIT_SHORT_SHA"
    paths:
      - db50x-dm42n-firmware.tgz
    expire_in: 30 days

# Release Package
build-release-package:
  stage: package
  image: fedora:latest
  tags:
    - docker
  dependencies:
    - build-dm42-firmware
    - build-dm32-firmware
    - build-dm42n-firmware
    - build-android
    - build-color-android
    - build-wasm
  script:
    - cd db48x-dm42-firmware && tar czf ../db48x-dm42-${CI_COMMIT_SHORT_SHA}.tar.gz * && cd ..
    - cd db50x-dm32-firmware && tar czf ../db50x-dm32-${CI_COMMIT_SHORT_SHA}.tar.gz * && cd ..
    - cd db50x-dm42n-firmware && tar czf ../db50x-dm42n-${CI_COMMIT_SHORT_SHA}.tar.gz * && cd ..
    - cd db48x-android && tar czf ../db48x-android-${CI_COMMIT_SHORT_SHA}.tar.gz * && cd ..
    - cd db50x-android && tar czf ../db50x-android-${CI_COMMIT_SHORT_SHA}.tar.gz * && cd ..
    - cd db48x-wasm && tar czf ../db48x-wasm-${CI_COMMIT_SHORT_SHA}.tar.gz * && cd ..
  artifacts:
    name: "release-packages-$CI_COMMIT_SHORT_SHA"
    paths:
      - db48x-dm42-*.tar.gz
      - db50x-dm32-*.tar.gz
      - db50x-dm42n-*.tar.gz
      - db48x-android-*.tar.gz
      - db50x-android-*.tar.gz
      - db48x-wasm-*.tar.gz
    expire_in: 90 days
  only:
    - stable
    - dev
